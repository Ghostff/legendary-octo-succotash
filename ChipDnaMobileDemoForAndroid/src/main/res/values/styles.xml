<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/card_default</item>
        <item name="colorPrimaryDark">@color/card_default</item>
        <item name="colorAccent">@color/card_default</item>
        <item name="android:windowBackground">@color/your_background_color</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
